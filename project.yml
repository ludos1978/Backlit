name: Backlit
options:
  bundleIdPrefix: com.backlit
  deploymentTarget:
    macOS: "14.0"

targets:
  Backlit:
    type: application
    platform: macOS
    deploymentTarget: "14.0"
    sources:
      - path: Backlit
        excludes:
          - "**/*.md"
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: io.github.ludos1978.backlit
        MARKETING_VERSION: "0.4.0"
        CURRENT_PROJECT_VERSION: "4"
        INFOPLIST_KEY_LSUIElement: true
        INFOPLIST_KEY_NSHumanReadableCopyright: "Backlit - Free & Open Source"
        SWIFT_VERSION: "6.0"
        CODE_SIGN_ENTITLEMENTS: Backlit/Backlit.entitlements
        ENABLE_HARDENED_RUNTIME: YES
        CODE_SIGNING_REQUIRED: YES
        CODE_SIGN_IDENTITY: "Apple Development"
        DEVELOPMENT_TEAM: 29W56PHWJR
        SWIFT_STRICT_CONCURRENCY: minimal
        SWIFT_OBJC_BRIDGING_HEADER: Backlit/Backlit-Bridging-Header.h
        GENERATE_INFOPLIST_FILE: YES
        INFOPLIST_KEY_NSScreenCaptureUsageDescription: "Backlit needs screen recording permission to show live previews of each display."
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
